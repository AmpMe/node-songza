"use strict";
/* global describe */
/* global it */

var promotion = require('../api/promotion');

var chai = require('chai');
chai.use(require('chai-as-promised'));
var expect = chai.expect;

describe('promotion', function() {

	describe('create', function() {
		// admin only
		it.skip('should work');
	});

});
